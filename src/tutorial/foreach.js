import { products } from "../products.js";

// for
// for (let i = 0; i < products.length; i++) {
//   console.log(products[i])
// }

// ForEach
// products.forEach(function (product, index, products) {
// // console.log(product, index, products)
//   console.log(product.name)
// })

// products.forEach((product, index, products) => console.log(product));

// products.forEach((product, index, products) => console.log(product.name));
